[pytest]
markers =
    integration: end-to-end smoke tests that spawn real node/exit/proxy processes. Excluded from the default run. Enable with `pytest -m integration`.
addopts = -m "not integration"
